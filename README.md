# Mobile Photography Gallery

Minimal React (Vite) skeleton for a dark, modern tile-based photo gallery with a lightbox.

## Scripts
- `npm install`
- `npm run dev`
- `npm run build`
- `npm run preview`

## Notes
- Replace the tile placeholders in `src/App.jsx` with real image data.
- The lightbox is wired for next/previous navigation.
